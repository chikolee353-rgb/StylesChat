using Microsoft.Maui.Controls;

namespace Client.Pages;

// Partial class backing the XAML ChatPage. UI elements are defined in ChatPage.xaml.
public partial class ChatPage : ContentPage
{
    public ChatPage()
    {
        InitializeComponent();
    }
}
